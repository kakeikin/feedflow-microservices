[pytest]
# This pytest.ini is for root-level integration tests only.
# Per-service unit tests have their own pytest.ini in each service directory
# and must be run from within that service directory (e.g. cd services/user-service && pytest).
testpaths = tests
